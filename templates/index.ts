export const firstAnswer = (input: string[]): number => {
	return 0;
};

// ----------------------------------------------------------------------

export const secondAnswer = (input: string[]): number => {
	return 0;
};
